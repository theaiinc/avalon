## Agent Notes

- The repository lives on the Data volume at `/Volumes/Data/llama-dash`.
- `/Users/stevetran/llama-dash` is a symlink to `/Volumes/Data/llama-dash` for compatibility with existing workspace references.
- The project is named Avalon. The frontend logo asset is `frontend/src/assets/avalon-logo.png`, copied from `/Users/stevetran/Downloads/avalon-logo.png`.
- Backend storage is configured in `backend/config.py`; it resolves to the repo-local `data/` directory (`/Volumes/Data/llama-dash/data`).
- GPU detection in `backend/gpu_detector.py` covers NVIDIA (`nvidia-smi`), macOS Metal via `system_profiler SPDisplaysDataType`, Windows Intel/NPUs via PowerShell, and Vulkan (`vulkaninfo`). On this machine the Metal detector reports `Apple M3 Pro` with Metal 4 and uses `sysctl -n hw.memsize` for unified memory.
- Agent-facing interfaces live in `backend/agent_cli.py` and `backend/mcp_server.py`. The CLI emits JSON and wraps the dashboard REST API on port `8771`; run it with `cd backend && .venv/bin/python agent_cli.py gpu-list`. The MCP server is stdio-based; run it with `LLAMA_DASH_URL=http://127.0.0.1:8771 .venv/bin/python mcp_server.py`.
- The frontend has an Agents page at `/agents` that shows MCP, CLI, and API connection commands for other agents.
- PC Links must use pairing-code flow, not manual URL-only linking: the local Avalon enters a pairing code, the remote Avalon completes pairing and returns/provides a unique API key for that linked PC. The API key must be saved in this local Avalon instance and used for all remote model/list/chat requests. Linked PC models appear in the local gateway as `pc:<pc_id>:<remote_model_id>`.
- The dashboard can start a separate local inference API on port `8787` with OpenAI-compatible `/v1/models` and `/v1/chat/completions` plus Anthropic-compatible `/v1/messages`; both the CLI and MCP server expose helpers for that lifecycle.
- The local inference API is a multi-model gateway. Downloaded models under `data/models/` automatically appear in `/v1/models`; callers select a model with the request `model` field. GGUF requests run via the active `llama-cli` backend driver; OpenVINO requests use cached `openvino_genai` pipelines.
- Aha: do not model the API Server UI as one running model on one device. The gateway can expose multiple local and linked-PC models at once; status should show a served-model catalog. GGUF models use `gguf_backend`/`gpu_index`, while OpenVINO models use `openvino_device`; keep legacy `device` only as a compatibility alias.
- Aha: keep the API Server Configuration card gateway-level only (port, API mode, start/stop). Do not put Quick Test model selection, GGUF backend buttons, or GPU device controls there; per-model serving details belong in served profiles and Quick Test owns its own model picker.
- Aha: each served model in `/v1/status` should include a serving profile, not just a model ID. Profiles include `served_id`, MTP and dSpark booleans inferred from model/repo/file names, driver/runtime, model file, and serving configuration such as API mode, port, backend, OpenVINO device, GPU index, default max tokens, and default temperature.
- Aha: serving profiles are editable. Overrides are stored in `data/served_profiles.json` through `backend/served_profiles.py` and keyed by the original `base_id`, so edited `served_id` aliases can still route to the underlying local model. The API page's Model Catalog card owns profile edit/save/reset UI.
- Aha: the dashboard backend may restart while the port `8787` gateway keeps running. The gateway exposes `/v1/status`, and `backend/api_server.py` uses it to recover UI fields (`model_id`, `port`, `mode`, `device`, `gpu_index`) instead of showing blank defaults for an already-running API server.
- Aha: this llama.cpp `llama-cli` build auto-enters interactive conversation mode when a chat template exists. Gateway subprocess calls must pass `--single-turn`, otherwise HTTP completions hang until the 120s timeout after generating the answer. Also use a helper for OpenVINO path detection; `any(False)` crashes GGUF requests.
- Aha: OpenAI-compatible streaming must be implemented in Avalon itself for local GGUF models. `backend/api_server.py` now returns `text/event-stream` for `stream:true` by reading `llama-cli` stdout incrementally, filtering startup UI/log output, and suppressing `[Start thinking]...[End thinking]` so clients receive only final assistant tokens. The first visible token can still be delayed until the model finishes its thinking block.
- Aha: the API Server page can read live gateway state directly from `http://localhost:<port>/v1/status`; the child gateway enables CORS and attaches per-model `current_request`/`last_request` stream metrics. The streamed token count is an estimate from generated model stdout before the thinking filter, because `llama-cli` does not expose exact token IDs or token boundaries through stdout.
- Aha: per-model stream metrics include `first_token_at_ms` and `first_token_latency_ms` for TTFT. If a `current_request` stops updating for more than 15s, `/v1/status` marks it `stale`, moves it to `last_request`, and clears the current stream so the dashboard does not stay stuck on Streaming.
- Aha: do not mark a stream stale solely because no tokens arrived for 15s. Some `llama-cli` requests keep running while loading/thinking before first stdout output. Stream metrics now attach the subprocess PID and only stale-clear a quiet request when the process is no longer alive.
- Aha: model catalog MTP state must match benchmark semantics. `mtp:true` means a base GGUF has a usable `MTP/*.gguf` draft head beside it; standalone artifacts whose name/path contains MTP but have no adjacent draft head are labeled `is_mtp_head:true` / "MTP head", not "MTP ready". API serving also adds `--spec-draft-model ... --spec-type draft-mtp` when `_find_mtp_head(model_file)` succeeds.
- Extra Gemma head artifacts downloaded for experiments: `amaranus_Gemma-4-E4B-it-qat-assistant-MTP-Q8_0-GGUF` (`gemma-4-E4B-it-qat-assistant-MTP-Q8_0.gguf`) and `ankk98_dspark-gemma4-12b-block7-Q4_0-GGUF` (`dspark_gemma4_12b_q4pure.gguf`).
- Aha: the repository has no `CONTRIBUTION_GUIDELINES.md`; use the existing `AGENT_GUIDELINES.md` and README for conventions until that file is added.
- Aha: completion contract tests live in `backend/tests`; mocked tests cover every local runtime format, while `AVALON_RUN_REAL_MODEL_TESTS=1` runs chat, Anthropic messages, and OpenAI streaming against every downloaded model.
- Aha: real model validation must execute chat, Anthropic messages, and streaming independently for each model and aggregate operation failures; otherwise one memory or runtime failure can hide the other completion paths.
- Aha: resource pressure queues new requests without cancelling existing reservations; gateway shutdown must refuse while active stream requests exist, otherwise an explicit stop can terminate an in-flight completion.
- Aha: accountless PC pairing now uses OS keyring-backed Ed25519 device identity and one-use five-minute codes; paired peer credentials are never stored in `pc_links.json`, and only `/api/pairing/accept` is LAN-reachable.
- Aha: dashboard API port 8771 and inference gateway port 8787 are separate processes. Stopping the dashboard backend does not automatically stop an orphaned gateway; verify and stop port 8787 independently. The gateway can launch concurrent `llama-cli` request subprocesses, which is risky on this 18 GB Apple-unified-memory Mac when multiple 2–5 GB models/requests are active.
- Aha: Electron must launch the dashboard sidecar on loopback and the gateway sidecar separately; packaged gateway startup uses `AVALON_GATEWAY_EXECUTABLE` because a PyInstaller backend executable cannot re-execute `api_server.py` through `sys.executable`. The gateway's public routes require an API key while dashboard and gateway management routes remain internal.
- Aha: local inference admission now estimates model weights, runtime overhead, KV cache, and MTP head cost before spawning `llama-cli`; low available memory or an oversized reservation waits briefly, then returns `503` with `Retry-After` and capacity details instead of risking a host freeze.
- Aha: keep separate logo assets for different surfaces. The app/UI icon retains the dark background with rounded corners, while the system tray asset uses the same mark with the background made transparent.
- Aha: release packaging is a cross-platform matrix; Linux electron-builder can exceed 25 minutes even when macOS and Windows finish, so the package timeout and watchdog window must leave Linux enough time to complete.
- Aha: electron-builder AppImage generation embeds a differential block map after squashfs creation; the large Linux sidecars make that step exceed 45 minutes, so Linux releases use the tar.gz target to keep the cross-platform release workflow finite.
- Aha: electron-builder tar.gz archives default to 7zip gzip level 9; Linux sidecars are large enough that this also exceeds the job budget, so the Linux tar.gz target uses `compression: "store"` for a fast, valid uncompressed gzip container.
- Aha: GitHub release assets must be below 2 GiB; Linux release packaging therefore overrides electron-builder with gzip level 5, balancing the sidecar tarball size against the 45-minute build timeout.
- Aha: if the compressed Linux tarball still exceeds GitHub's 2 GiB per-asset limit, the release job splits it into 1900 MiB parts and publishes reconstruction instructions alongside the release manifest.
- Aha: the release watchdog must ignore its own job when checking completion; waiting for the workflow status to become completed creates a self-deadlock and eventual cancellation.
- Aha: packaged Electron pages load through `file://`; Vite must use `base: "./"` or production CSS and JavaScript resolve to `/assets/...` and the app renders as a blank window.
- Aha: packaged Electron `app.getAppPath()` is the `app.asar` file; never use it as a sidecar child-process cwd. Use `process.resourcesPath` so the backend can start and GPU detection/API calls do not appear empty.
- Aha: driver downloads need bounded GitHub metadata/asset timeouts and a `.part` file renamed only after completion; otherwise a stalled request can remain at "Fetching release info" or leave an archive that later fails extraction.
- Aha: multimodal testing is isolated from llama.cpp benchmarks behind the versioned `avalon.multimodal/v1` contract. Profiles reference allowlisted executable IDs or validated HTTP targets; test cases and runs are separate, artifacts are copied into run-scoped storage, and secret references use `env:NAME` instead of persisted credentials.
- Aha: multimodal runs are independent worker threads and downloads are detached worker processes with per-job persisted progress, so multiple tests and downloads can run concurrently without relying on one dashboard task.
- Aha: image-capable local models default to the built-in stable-diffusion.cpp adapter instead of the LLM gateway; first use provisions `sd-cli` plus FLUX VAE/CLIP-L/T5-XXL dependencies under `data/runtimes/stable-diffusion-cpp/`.
- Aha: the Multimodal create form keeps the selected run ID and renders its live state/result in a right-column canvas; polling the existing run list is sufficient to update the canvas without adding a second run-stream API.
- Aha: multimodal run records expose a persisted progress stage/detail and adapter timeout; the UI can show truthful activity and elapsed time while keeping ETA explicitly model/hardware dependent.
- Aha: downloaded GGUF models are represented by Avalon as model directories, but stable-diffusion.cpp requires the actual diffusion `.gguf` file; the built-in image runtime must resolve a directory to its primary GGUF before invoking `sd-cli`.
- Aha: Electron loads the production frontend from `file://.../frontend/dist`; UI changes require rebuilding the frontend and relaunching Electron, and the multimodal canvas falls back to the latest active run so it remains visible after reloads.
- Aha: the multimodal form must use an unconditional two-column grid because the compact Electron window can be narrower than Tailwind's `md` breakpoint; responsive `md:grid-cols-2` otherwise places the canvas below the form.
- Aha: if grid sizing still collapses in the packaged/file-based Electron renderer, use explicit `flex-row` with `w-1/2` children for the create-form/canvas split.
- Aha: artifact URLs returned by the dashboard are `/api/...` paths; Electron's `file://` renderer needs them prefixed with `http://127.0.0.1:8771` before opening history artifacts or previewing images.
- Aha: Save-and-run must deduplicate both profiles and cases; otherwise each repeated submission gets a new profile ID and prevents case-level parameter matching.
- Aha: the form/canvas JSX column wrapper must stay open through all form controls; closing it after the model selector makes later controls become sibling flex items and pushes the canvas below.
- Aha: run status polling must request `/api/multimodal/runs` independently; polling profiles, cases, capabilities, and local models with `Promise.all` can delay or suppress completed canvas/history updates when a slower endpoint is unavailable.
- Aha: Electron production pages load from `file://`; React `BrowserRouter` turns `/multimodal?model=...` into an invalid `file:///multimodal` navigation. Use `HashRouter` for `file:` and `BrowserRouter` for HTTP.
- Aha: with `HashRouter`, route query parameters must be read via React Router's `useLocation().search`; `window.location.search` remains empty for model-card links such as `#/multimodal?model=...`.
- Aha: Avalon MCP now mirrors the multimodal REST lifecycle: capabilities, profile/case CRUD, run creation/status/cancellation, and progress/result/artifact metadata are exposed through `mcp_server.py` and `DashboardClient`.
- Aha: agents need a high-level `run_multimodal_test` MCP tool to avoid incorrectly selecting generic chat for FLUX/audio/video models; `chat_completion_openai` now rejects recognizable multimodal model IDs with a routing hint.
- Aha: because agents may still select an older generic chat schema, `chat_completion_openai` must defensively reroute recognized multimodal model IDs to the multimodal runner instead of forwarding them to the LLM gateway.
- Aha: imagegen cases now carry an `options` object for negative prompt, dimensions, steps, guidance, seed, and optional `image_base64`; the built-in sd-cli adapter maps these to stable-diffusion.cpp flags including `--init-img`.
- Aha: legacy MCP clients may only expose `chat_completion_openai`; that compatibility tool must itself advertise reference-image and image-preference fields and route FLUX/Kontext calls to the multimodal runner.
- Aha: Qwen-Image-Edit-2511 GGUF is not FLUX-compatible; stable-diffusion.cpp needs the Qwen2.5-VL GGUF text encoder, Qwen image VAE, `--flow-shift 3`, `--qwen-image-zero-cond-t`, and a reference image passed with `-r`.
- Aha: released `sd-cli` builds may lag Qwen support; probe `sd-cli --help` before adding `--qwen-image-zero-cond-t`, otherwise Qwen runs fail during argument parsing. Omitting it preserves execution but may reduce 2511 edit quality.
- Aha: built-in diffusion runs must use tracked `Popen` process groups, not `subprocess.run`; otherwise `/cancel` can only mark a run `cancelling` while the image process continues until timeout.
- Aha: after switching diffusion execution to `Popen.communicate()`, use its returned stdout/stderr strings for result metadata; `Popen.stdout` remains a non-subscriptable `TextIOWrapper`.
- Aha: dashboard startup now supervises the local inference gateway for script-based launches as well as Electron. It auto-starts a missing gateway and retries unexpected exits, while an explicit API Stop suppresses recovery until the dashboard is restarted.
- Aha: packaged Electron builds must include both native Python sidecars. `npm run pack`/`dist` now checks for them and builds missing sidecars before electron-builder; without them a packaged app cannot launch the dashboard backend from inside `app.asar`.
- Aha: an "indefinitely hanging" completion with a healthy `/v1/models` is usually memory-pressure admission control, not a wedged runtime: `resource_safety.acquire` queues the request silently (no bytes on the wire) until memory clears or the max wait expires into a 503. Check `/v1/status` `resources.events` for `request_delayed`. The default max wait is 30s (`AVALON_RESOURCE_MAX_WAIT_SECONDS`); it was 300s, which made overload indistinguishable from a hang for any client with a shorter timeout. Admission needs `available >= estimate + 3072 MB reserve` (`AVALON_MEMORY_RESERVE_MB`), so on the 18 GB Mac even a 2B model is refused when other apps hold memory.
- Aha: PyInstaller onefile sidecars always run as a bootloader parent plus a worker child, so `ps` shows two `avalon-backend` and two `avalon-gateway` processes per instance; that pairing is normal, not a duplicate-spawn bug. The gateway pair is parented by the backend worker via the startup supervisor.
- Aha: raw `fetch('/api/...')` calls in the frontend silently fail in the packaged Electron renderer because pages load from `file://`; always go through `api`/`client.ts` (or a `dashboardBase` with a loopback fallback) so requests reach `http://127.0.0.1:8771`.
- Aha: llama.cpp b9947's `llama-cli` launches an embedded `llama_server`; its generic `llama_server exited with code 1` means the useful cause is earlier in verbose stderr. Existing Avalon GGUF downloads can be truncated/corrupt and fail with `tensor ... data is not within the file bounds`; redownload the affected model before debugging Metal or gateway arguments.
